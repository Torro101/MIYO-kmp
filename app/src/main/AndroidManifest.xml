<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools">

	<!-- Permissions, activities, services, receivers are declared in the shared module -->
	<application
		android:name="org.koharu.miyo.MiyoApp"
		android:allowBackup="true"
		android:backupAgent="org.koharu.miyo.backups.domain.AppBackupAgent"
		android:dataExtractionRules="@xml/backup_rules"
		android:enableOnBackInvokedCallback="@bool/is_predictive_back_enabled"
		android:fullBackupContent="@xml/backup_content"
		android:fullBackupOnly="true"
		android:hasFragileUserData="true"
		android:restoreAnyVersion="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:largeHeap="true"
		android:localeConfig="@xml/locales_config"
		android:networkSecurityConfig="@xml/network_security_config"
		android:requestLegacyExternalStorage="true"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/Theme.Miyo"
		tools:ignore="UnusedAttribute" />
</manifest>
